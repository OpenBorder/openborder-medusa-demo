PORT=8000
DEMO_MODE=preview

# Optional local Test-mode integration. The server rejects live keys.
# 1. Set DEMO_MODE=api.
# 2. Add your Test secret key on the server and Test publishable key for the browser element.
# 3. Keep this file as documentation; put real values in ignored .env only.
# OPENBORDER_API_URL=https://your-supported-test-api.example
# OPENBORDER_API_KEY=<your-test-secret-key>
# OPENBORDER_PUBLISHABLE_KEY=<your-test-publishable-key>
